# Synthetic MOF worked example

This v0.3.3-alpha release-candidate example distinguishes recorded source links, file integrity, metadata validation and scientific assessment. It is entirely synthetic.

```bash
python3 -m pip install -r requirements.txt
python3 validate_mof_worked_example.py
python3 -m unittest discover -s tests -v
```
