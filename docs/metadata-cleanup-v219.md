# v219 metadata cleanup note

The live Zenodo v0.3.2-alpha record minted DOI `10.5281/zenodo.20274154` after the repository was enabled through the Zenodo GitHub integration.

The previous metadata title contained the phrase "MCT". For the v219 package, the repository metadata files have been aligned to the lower-risk title:

`MCT provenance credential prototype for AI-ready MOF data`

This preserves the MCT acronym while emphasizing a non-financial provenance-credential prototype. If the live Zenodo page must be changed as well, update the GitHub repository metadata and either edit the Zenodo record metadata if allowed or create an optional `v0.3.2-alpha` metadata-cleanup release.
