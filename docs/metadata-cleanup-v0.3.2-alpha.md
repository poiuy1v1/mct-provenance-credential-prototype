# v0.3.2-alpha metadata cleanup QC

Purpose: make the Zenodo-facing title and metadata less likely to be interpreted as a financial token, cryptocurrency, investment product, or deployable governance platform.

Clean title used for Zenodo/GitHub-facing metadata:

```text
MCT provenance credential prototype for AI-ready MOF data
```

This release does not change the Python simulation, JSON schema logic, notebook workflow, synthetic data, generated outputs, Solidity stub semantics, or GitHub Actions smoke test.

Known DOI status:

- Prior manuscript-linked DOI: `10.5281/zenodo.20274154`
- New v0.3.2-alpha DOI: to be copied from Zenodo after release processing

Recommended GitHub release title:

```text
v0.3.2-alpha: Metadata cleanup for MCT provenance credential prototype
```

Recommended release notes: use `RELEASE_NOTES_v0.3.2-alpha.md`.
