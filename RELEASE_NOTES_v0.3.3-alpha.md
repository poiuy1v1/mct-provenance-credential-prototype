# v0.3.3-alpha: executable artifact alignment

This candidate adds modular JSON Schema composition, a synthetic UiO-66 domain profile, exact layered validation fields, an executable notebook, release-level validation and negative tests. File integrity and source-link recording remain distinct from scientific review. All records are synthetic and `scientific_assessment.status` is `not_reviewed`.
